import styled from "styled-components";

const PageContainer = styled.div`
  display: flex;
  flex-direction: row;
  overflow: hidden;

  width: 100vw;
  height: 100vh;
`;

export default PageContainer;
